#include <vector>
#include <array>

class Model{


    Model

    Model(const Model&) = delete;
    Model(const Model&&) = delete;
    
    
    void operator=(const Model&) = delete;
    void operator=(const Model&&) = delete;


};