#include <iostream>
#include "model.h"
#include "../lib/gmtl/Output.h"
int main(){
    // Model mdl("../res/head.obj");
    // mdl.readmodel();
    // mdl.normalize(true);
    // for (gmtl::Trif tri : mdl.getmodel())
    //     std::cout << tri << std::endl;
}

